export const noteFileName = (noteId: string): string => `${noteId}.json`;
export const noteMarkdownName = (noteId: string): string => `${noteId}.md`;

export const templateFileName = (templateId: string): string =>
  `${templateId}.json`;
export const templateMarkdownName = (templateId: string): string =>
  `${templateId}.md`;
